# LeetLoop Extension Environment Variables
# Copy to .env and fill in your values

# Supabase configuration (fallback if API_URL not set)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Backend API URL (recommended - routes submissions through API)
# When set, the extension syncs submissions via the API instead of directly to Supabase
API_URL=https://leetloop-xxxxx.run.app
