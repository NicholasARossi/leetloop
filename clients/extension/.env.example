# LeetLoop Extension Environment Variables
# Copy to .env and fill in your values

# Backend API URL (required - all data operations route through the API)
API_URL=https://leetloop-api-xxxxx.run.app

# Web app URL for auth redirects and web-bridge (defaults to http://localhost:3001)
WEB_APP_URL=https://leetloop-web-xxxxx.run.app
